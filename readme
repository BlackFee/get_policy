运行之前，需要做以下操作：
1、请使用以下命令安装脚本运行所需的第三方库：
	pip install -r requirements.txt
2、需要修改settings.py中的url参数，请将主机地址（host参数）修改为需要爬取的主机地址。
3、需要修改当前登录web界面的cookie值。否则会报未登录的访问错误提示。

说明：
1、此脚本未启用https验证
2、使用时需检查系统版本，由于不同系统下API接口存在差异，如version11版本与version版本则不一致。
3、未解决策略名字中存在反省杠的问题（这些策略名字无法进行API接口访问，最终生成的excel中会记录未爬取的策略名字）
4、爬取日志会记录在log_<date>文件里面，<date>代表当前爬取时间格式为：年-月-日-时分-秒
5、爬取的策略详细信息会保存在“all_policy_<date>.xlsx”文件里。